import React from 'react';
import ReactDOM from 'react-dom';
import Layout from './componentes/component';

const container = document.getElementById('root');

ReactDOM.render( <Layout/ > , container);